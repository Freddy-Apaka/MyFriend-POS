// @myfriend-pos/ui — shared component library.
//
// Intentionally empty at Phase 2. Real components are added starting in M2
// once the UI/UX Screen Specification is implemented against actual screens.
// The placeholder export below exists only so this package has a valid
// entry point and the monorepo build graph resolves correctly.

export const UI_PACKAGE_VERSION = '0.1.0'
